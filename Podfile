# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PhotoZigChallenge' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  #Não preciso justificar a utilização santissima trindade do desenvolvimento i$
  pod 'Alamofire'
  pod 'ObjectMapper'
  pod 'RealmSwift'

  pod 'Kingfisher', '~> 4.0'
  #Auxiliar na utilização de Objetos representados em JSON
  pod 'SwiftyJSON'
  pod 'AlamofireObjectMapper', '~> 5.0'

  # Pods for PhotoZigChallenge

end
